# Changelog

## Unreleased

- Require `servant-client-core-0.21` and render its partially escaped query
  representation with the official `http-types` renderer.

- Add typed fetch request construction, retry policy, and failure handling.
- Add unit and real-reactor contract tests.
